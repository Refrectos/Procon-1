<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>PRoCon.UI</RootNamespace>
    <AssemblyName>PRoCon.UI</AssemblyName>
    <OutputPath>..\..\builds\$(Configuration)\</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <AppendRuntimeIdentifierToOutputPath>false</AppendRuntimeIdentifierToOutputPath>
    <BuiltInComInteropSupport>true</BuiltInComInteropSupport>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
    <ApplicationIcon>procon.ico</ApplicationIcon>
    <!-- Single-file self-contained publish: one .exe, no loose DLLs -->
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>true</SelfContained>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
    <DebugType>embedded</DebugType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.2.3" />
    <PackageReference Include="Avalonia.Desktop" Version="11.2.3" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.2.3" />
    <PackageReference Include="Avalonia.Fonts.Inter" Version="11.2.3" />
    <PackageReference Include="Avalonia.ReactiveUI" Version="11.2.3" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\PRoCon.Core\PRoCon.Core.csproj" />
    <ProjectReference Include="..\PRoCon.Themes\PRoCon.Themes.csproj" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <AvaloniaResource Include="procon.ico" />
    <AvaloniaResource Include="Assets\myrcon_logo.png" />
  </ItemGroup>

  <!-- Exclude plugin-referenced DLLs from single-file bundling so they remain
       as loose files for Roslyn plugin compilation references -->
  <Target Name="ExcludePluginDepsFromBundle" AfterTargets="ComputeResolvedFilesToPublishList"
          Condition="'$(PublishSingleFile)' == 'true'">
    <ItemGroup>
      <ResolvedFileToPublish Update="@(ResolvedFileToPublish)"
        Condition="%(Filename) == 'PRoCon.Core' Or
                   %(Filename) == 'MySqlConnector' Or
                   %(Filename) == 'Newtonsoft.Json' Or
                   %(Filename) == 'Dapper' Or
                   %(Filename) == 'Flurl' Or
                   %(Filename) == 'Flurl.Http' Or
                   %(Filename) == 'Microsoft.Data.Sqlite' Or
                   %(Filename) == 'SQLitePCLRaw.core' Or
                   %(Filename) == 'SQLitePCLRaw.provider.e_sqlite3' Or
                   %(Filename) == 'SQLitePCLRaw.batteries_v2'">
        <ExcludeFromSingleFile>true</ExcludeFromSingleFile>
      </ResolvedFileToPublish>
    </ItemGroup>
  </Target>

</Project>
